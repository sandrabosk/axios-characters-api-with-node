<div class="container mt-5">
    <h2> Character {{character.name}} </h2>
    <hr>
    <ul>
        {{#each characters }}
        <li> <a href="/characters/list/{{id}}">{{ name }} </a></li>
        {{/each}}
    </ul>
</div>
