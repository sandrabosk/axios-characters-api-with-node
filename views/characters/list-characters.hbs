<div class="container mt-5">

    <div class="row">

        {{#each characters}}

        <div class="col-md-4">

            <article class="character-card mb-5">

                <h4>{{name}}</h4>
                <hr>
                <p>Identificador: {{id}}</p>
                <p>Profesión: {{occupation}}</p>
                <p>Arma: {{weapon}}</p>

                <div class="d-grid">

                    <div class="btn-group">
                        <a href="/characters/details/{{id}}" class="btn btn-dark btn-sm">Ver detalles</a>
                        <a href="/characters/edit/{{id}}" class=" btn btn-warning btn-sm">Editar</a>
                        <a href="/characters/delete/{{id}}" class="btn btn-danger btn-sm">Eliminar</a>
                    </div>

                </div>

            </article>

        </div>
        {{/each}}
    </div>

</div>