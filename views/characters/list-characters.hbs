<div class="container mt-5">

    <div class="row">

        {{#each characters}}

        <div class="col-md-3">

            <article class="character-card mb-5">

                <h4><strong>{{name}}</strong></h4>
                <hr>
                <p>Identificador: {{id}}</p>
                <p>Profesión: {{occupation}}</p>
                <p>Arma: {{weapon}}</p>
                <p>Deuda: {{debt}}</p>

                <div class="d-grid">

                    <a href="/details/{{id}}" class="btn btn-primary">Ver detalles</a>
                    <a href="/edit/{{id}}" class="btn btn-warning">Editar</a>
                    <form action="" method="POST">
                        <a href="/delete/{{id}}" class="btn btn-danger">Eliminar</a>
                    </form>

                </div>

            </article>

        </div>
        {{/each}}
    </div>

</div>