<div class="container mt-5">

    <div class="row">

        {{#each characters}}

        <div class="col-md-3">

            <article class="character-card mb-5">

                <h4>{{name}}</h4>
                <hr>
                <p>Identificador: {{id}}</p>
                <p>Profesión: {{occupation}}</p>
                <p>Arma: {{weapon}}</p>

                <div class="d-grid">

                    <div class="btn-group">
                        <a href='/character/characters/{{id}}' class="btn btn-dark btn-sm">Ver detalles</a>
                        <a href='/character/{{id}}/edit' class="btn btn-warning btn-sm">Editar</a>
                    </div>

                </div>

            </article>

        </div>
        {{/each}}
    </div>

</div>