<h2 style="margin-top:20px">Character's details</h2>


<div class="card text-center mt-4" style="width: 18rem;">

    <div class="card-body">
        <h1 class="card-title">Name:<br>{{character.name}}</h1>
        <hr>
        <h4>Weapon: {{character.weapon}}</h4>
        <hr>
        <p class="card-text">Occupation: {{character.occupation}}</p>
        <dir class="container">
            <a href="/characters/{{character.id}}/edit" class="btn btn-dark" style="margin-top:10px">Editar el
                personaje</a>
            <form action="/characters/{{character.id}}/delete" method="POST">
                <button type="submit" class="btn btn-warning btn-lg mt-4">Eliminar personaje</button>
            </form>



        </dir>
    </div>